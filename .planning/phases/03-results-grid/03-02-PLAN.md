---
phase: 03-results-grid
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - internal/web/server.go
  - internal/web/templates/links.html
  - internal/web/templates.go
  - internal/web/static/style.css
  - internal/web/server_test.go
autonomous: true
requirements: [ADM-01]
user_setup: []

estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ADM-01 access parity
    - "GET /poll/{ptoken}/admin/{atoken} renders the same results grid (same data, same partial, same view-model) as the participant route, appended below the share-links card (ADM-01)"
    - "The admin route's grid exposes no data beyond what the participant route shows — no admin/cookie/participant tokens leak into the grid (threat T-03-02)"
    # CSS state coverage (backstops from UI-SPEC UI Considerations)
    - "Yes/No/Maybe cells render as tri-color badges reusing the existing tokens (--color-yes green, --color-destructive red, --color-text-muted gray) — no new hex values introduced"
    - "Best-fit row(s) get the existing accent-tint treatment (#EFF6FF background + #2563EB left border on the sticky column) plus the 'Best fit' text badge — color is never the only signal"
    - { statement: "On a narrow (~360px) viewport the grid scrolls horizontally with the slot-label column pinned via position:sticky/left:0 so it stays visible while the participant columns scroll", verification: backstop }
    - { statement: "A participant display name longer than the column-header width truncates with an ellipsis at max-width 140px and the full name remains recoverable via the native title attribute", verification: backstop }
  artifacts:
    - "results-table / results-scroll / results badge / results-row-best CSS rules in internal/web/static/style.css"
    - "results partial invocation in internal/web/templates/links.html"
  key_links:
    - "handleLinksPage → store.ResponsesByPollID → buildResultsGridView → results.html partial (identical chain to handleVoteForm, ADM-01 parity)"
    - "results.html registered as a shared partial in templates.go's links page composition (added to the links template's ParseFS file list)"
    - "results.html class hooks (results-table, results-scroll, results badges, results-row-best) ↔ style.css rules added here"
---

<objective>
Extend the Phase 3 results grid to the admin route and give it its visual form. Plan 03-01 built
the grid, ranking, comments, and all data states on the participant route; this plan (1) renders
the identical grid on `GET /poll/{ptoken}/admin/{atoken}` so an organizer sees results via the
secret admin link (ADM-01), and (2) adds the grid's CSS — tri-color badges, sticky first column +
horizontal scroll, and the best-fit highlight — reusing existing tokens only.

Purpose: complete the phase's demoable slice (anyone with EITHER link sees the full styled grid)
and satisfy ADM-01 access parity.
Output: results grid on the admin route + grid CSS in `style.css`.

Reversibility: additive handler wiring + a new CSS block; no schema/data change, fully reversible.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-results-grid/03-CONTEXT.md
@.planning/phases/03-results-grid/03-UI-SPEC.md
@.planning/phases/03-results-grid/03-01-SUMMARY.md

# Existing code this plan extends (read before touching):
@internal/web/server.go
@internal/web/templates.go
@internal/web/templates/links.html
@internal/web/templates/results.html
@internal/web/static/style.css
@internal/web/server_test.go
</context>

## Artifacts this phase produces (Plan 03-02)

- **HTTP route behavior change:** `GET /poll/{ptoken}/admin/{atoken}` (`handleLinksPage`) now renders the results grid below the share-links card. No new routes.
- **Go — web (`internal/web/server.go`):** `handleLinksPage`'s view struct gains a `Results resultsGridView` field, populated via the same `ResponsesByPollID` + `buildResultsGridView` chain used by `handleVoteForm`.
- **Template:** `internal/web/templates/links.html` invokes `{{template "results" .Results}}`; `results.html` registered for the links page in `templates.go`.
- **CSS (`internal/web/static/style.css`):** `.results-section`, `.results-scroll`, `.results-table`, sticky first-column rule, `.results-badge`/`.results-badge-yes|-no|-maybe`, missing-cell muted style, `.results-row-best` tint + border, `.results-best-badge` (all reusing existing `--color-*` tokens).

<tasks>

<task type="auto">
  <name>Task 1: Admin-route results-grid parity (ADM-01)</name>
  <files>internal/web/server.go, internal/web/templates/links.html, internal/web/templates.go, internal/web/server_test.go</files>
  <read_first>
    - internal/web/server.go — `handleLinksPage` (uses `PollByTokens`, builds an anonymous view struct passed to `s.tmpl.links`); `buildResultsGridView`, `ResponsesByPollID`, and `resultsGridView` from Plan 03-01.
    - internal/web/templates.go — the `links` template's `ParseFS` file list (add results.html the same way 03-01 added it to `vote`).
    - internal/web/templates/links.html — the `{{define "content"}}` card; the results partial is invoked after the existing share-links card, still inside "content".
    - internal/web/templates/results.html — the "results" partial and the `.Results` shape it expects.
    - internal/web/server_test.go — `TestLinksPage_RendersBothLabelsAndAbsoluteURLs` and `TestVote_EndToEnd_SubmitAndRevisit` for the admin-URL + response-submission patterns to reuse.
    - .planning/phases/03-results-grid/03-CONTEXT.md — Page Placement & Access Parity ("admin link shows the identical grid … no admin-only controls yet") and the Deferred Ideas (no admin-only visual distinction this phase).
  </read_first>
  <action>
    In `handleLinksPage` (server.go): after the existing `PollByTokens` lookup succeeds, call `s.store.ResponsesByPollID(r.Context(), poll.ID)` then `buildResultsGridView(poll.PollType, slots, participants, answers)`, and add the resulting `resultsGridView` as a `Results` field on the anonymous struct already passed to `s.tmpl.links.ExecuteTemplate`. Reuse the slots already returned by `PollByTokens` (no second slot query). On a store error, use the handler's existing `http.StatusInternalServerError` fallback branch. Introduce NO admin-only columns, controls, or extra fields — the admin grid must be byte-identical in structure to the participant grid (CONTEXT.md parity decision; admin-only distinction is a Deferred Idea).

    templates.go: add `templates/results.html` to the `links` template's `ParseFS` file list so the "results" define resolves on the links page.

    links.html: invoke `{{template "results" .Results}}` immediately after the existing share-links `</div>` card and before the closing of `{{define "content"}}`, so the grid renders below the links (the links page has no vote form, so the grid is the only results surface there).

    server_test.go: add `TestResults_AdminRouteParity` — create a poll, submit responses for two participants over HTTP on the participant route, then GET `/poll/{ptoken}/admin/{atoken}` and assert 200 and that the admin body contains the same grid signals the participant page shows (both participant names, the ✓ badge glyph, a slot tally line, and the `Best fit` badge). Optionally assert the admin body does NOT contain the raw admin token string inside any grid cell/header (parity/no-leak).
  </action>
  <verify>
    <automated>cd /Users/cfiguerola/src/date-chooser && go build ./... && go vet ./... && go test ./internal/web/ -run 'Results|Links' -v</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && grep -q 'template "results"' internal/web/templates/links.html && echo "admin-grid-wired-ok"</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && ! grep -rn --include='*.go' 'template.HTML' internal/web/ && echo "XSS-gate-ok (T-03-01)"</automated>
  </verify>
  <done>
    ADM-01 holds: `GET /poll/{ptoken}/admin/{atoken}` renders the identical results grid (same partial, same view-model) below the share-links card; `TestResults_AdminRouteParity` passes proving the admin body carries the same names/badges/tally/best-fit signals as the participant route; `go build`/`go vet`/the `Results|Links` tests pass; and no admin-only data leaks into the grid.
  </done>
  <reversibility rating="reversible">Additive read-only wiring into an existing handler + partial include; no schema/data change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Results-grid CSS — badges, sticky column, best-fit highlight, responsive scroll</name>
  <files>internal/web/static/style.css, internal/web/server_test.go</files>
  <read_first>
    - internal/web/static/style.css — the `:root` token block (`--color-yes #16A34A`, `--color-destructive #DC2626`, `--color-text-muted #64748B`, `--color-accent #2563EB`, `--color-surface`, `--color-border`, `--space-*`), the `.card`/`.heading`/`.label` rules, the existing `.banner-success` (`#EFF6FF` tint + accent border) and `.pill` tri-color `:checked` rules to mirror the badge coloring approach and reuse the accent-tint combo.
    - internal/web/templates/results.html — the exact class names emitted by Plan 03-01 (`results-section`, `results-scroll`, `results-table`, the badge span classes, `results-row-best`, the Best-fit badge span, the sticky slot-label cell) so the CSS selectors match the real markup.
    - .planning/phases/03-results-grid/03-UI-SPEC.md — the Grid Structure section (sticky first `<th>/<td>` `position:sticky;left:0`; header `max-width:140px`+ellipsis+`title`; data cell badge `min-width/height 28px`, `border-radius 6px`, 14px/600 white text; best-slot `background:#EFF6FF` + `border-left:3px solid #2563EB` on the sticky column only; 960px results wrapper) and the Spacing/Color/Typography tables (reuse only existing tokens/sizes/weights — zero new hex, no new font size or weight).
    - .planning/WINDOWS.md — the existing unrun-verify entries (#7, #8) format, to append the two browser-only backstop entries this task creates.
  </read_first>
  <action>
    Add a results-grid CSS block to style.css, reusing ONLY existing tokens (no new hex, no new font size/weight — UI-SPEC budget lock):
    - `.results-section`: the 960px-max-width wrapper (breaks out of the 640px `.page` on desktop, full-width minus padding on mobile), with `--space-md` top gap from the form/links card above; a `.heading`-consistent "Results"/"Comments" heading rhythm using `--space-2xl` above.
    - `.results-scroll`: `overflow-x: auto;` horizontal-scroll container.
    - `.results-table`: `border-collapse: collapse;` full-width table; header row background `--color-surface`, bottom border `1px solid var(--color-border)`; cells padded `--space-sm`; data-cell `min-width: 48px`.
    - Sticky slot-label column: first `th`/`td` in each row `position: sticky; left: 0; background: var(--color-surface); min-width: 168px;` with a `1px solid var(--color-border)` right border so scrolling content never shows through.
    - Participant header cells: `max-width: 140px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` (full name lives in the `title` attribute from Plan 03-01).
    - Badges: `.results-badge` base — `inline-flex; align-items:center; justify-content:center; min-width:28px; min-height:28px; border-radius:6px; color: var(--color-surface); font-size:14px; font-weight:600;`. Variants: `.results-badge-yes { background: var(--color-yes); }`, `.results-badge-no { background: var(--color-destructive); }`, `.results-badge-maybe { background: var(--color-text-muted); }`.
    - Missing-cell text ("—"): muted color `var(--color-text-muted)`, no background (visually distinct from the Maybe badge, per CONTEXT.md).
    - Best-fit row: `.results-row-best` cells `background: #EFF6FF;` (reuse the existing accent-tint value used by `.banner-success`/secret-link box); the sticky label cell of a best row gets `border-left: 3px solid var(--color-accent);`. `.results-best-badge`: accent text on the tint (mirror the existing accent-on-tint label pattern), reusing 14px/600.
    - Tally line: 14px/400 muted (recombination of existing size/weight — no new token).

    Do NOT change any existing selector's declarations; only append new `.results-*` rules.

    Record the two browser-only backstops in .planning/WINDOWS.md as `unrun-verify` entries (matching entries #7/#8 shape): (a) sticky slot-label column stays visible while scrolling at ~360px, and (b) header-name ellipsis truncation triggers for a long name with the full name recoverable via the title attribute. These are CSS-render facts Go httptest cannot observe (UI-SPEC marks both as 🧪 backstop).
  </action>
  <verify>
    <automated>cd /Users/cfiguerola/src/date-chooser && go build ./... && go test ./internal/web/ -run 'Results' -v</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && grep -Eq 'position:\s*sticky' internal/web/static/style.css && grep -q 'results-badge-yes' internal/web/static/style.css && grep -q 'results-row-best' internal/web/static/style.css && echo "grid-css-present-ok"</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && test "$(grep -oE '#[0-9A-Fa-f]{6}' internal/web/static/style.css | grep -viE '#F8FAFC|#FFFFFF|#2563EB|#DC2626|#16A34A|#1E293B|#64748B|#E2E8F0|#EFF6FF' | sort -u | wc -l | tr -d ' ')" = "0" && echo "no-new-hex-ok"</automated>
  </verify>
  <done>
    The grid is fully styled from existing tokens only: `go build` and the `Results` tests still pass; style.css contains the sticky-column rule, tri-color badge variants, and the best-fit tint/border/badge; the no-new-hex gate confirms no color outside the established Phase 1/2 palette was introduced; and the two browser-only render backstops (sticky column at 360px, header ellipsis) are recorded in WINDOWS.md for a later UI-review pass.
  </done>
  <reversibility rating="reversible">Append-only CSS + a ledger note; no behavior/schema change.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin link holder → server (read) | The admin route now renders participant-submitted names/comments (same stored-XSS surface as the participant route) and must not expose data the participant route hides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering (stored XSS) | admin-route render of `results.html` (`internal/web/server.go handleLinksPage`, `links.html`) | high | mitigate | Admin route reuses the exact same `results.html` partial + `buildResultsGridView` view-model as the participant route — all participant text still flows through html/template auto-escaping. No new rendering path is introduced. Same negative grep gate enforced in `<verify>`. |
| T-03-02 | Information disclosure | `handleLinksPage` results wiring (`internal/web/server.go`) | medium | mitigate | The admin grid is populated from the identical `resultsGridView` (DisplayName/Answer/Comment/counts only). No admin-only columns/fields are added (CONTEXT.md parity decision). Test asserts the admin token string does not appear inside grid cells/headers. |
| T-03-04 | Information disclosure | grid CSS (`style.css`) | low | accept | CSS is presentational only; it renders no data and reuses existing tokens. No new data exposure. |

No package-manager installs occur in this phase; the package-legitimacy gate is not applicable.
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` clean.
- `go test ./internal/web/ -run 'Results|Links' -v` passes, including `TestResults_AdminRouteParity`.
- Admin route renders the same grid signals as the participant route (names, badges, tally, best-fit).
- style.css contains the sticky-column, tri-color badge, and best-fit rules; no-new-hex gate passes.
- Two browser-only backstops (sticky column at ~360px; header ellipsis + title recoverability) recorded in WINDOWS.md as unrun-verify — these require a later real-browser UI-review pass, consistent with Phase 2's precedent.
</verification>

<success_criteria>
An organizer opening the secret admin link (`GET /poll/{ptoken}/admin/{atoken}`) sees the same results grid participants see (ADM-01), and the grid is visually complete: tri-color badges, per-slot tallies, a sticky slot-label column with horizontal scroll on narrow viewports, and the best slot(s) highlighted with an accent tint + "Best fit" badge — all using the established Phase 1/2 color/type/space tokens with zero new values.
</success_criteria>

<output>
Create `.planning/phases/03-results-grid/03-02-SUMMARY.md` when done.
</output>
