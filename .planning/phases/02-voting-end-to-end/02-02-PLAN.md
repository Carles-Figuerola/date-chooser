---
phase: 02-voting-end-to-end
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - internal/web/server.go
  - internal/web/templates/vote.html
  - internal/web/templates/notfound.html
  - internal/web/templates.go
  - internal/web/server_test.go
autonomous: true
requirements: [VOTE-01, VOTE-02, VOTE-03]

estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A blank display name is rejected with the inline error 'Your name is required.' plus the banner 'Check the highlighted fields and try again.', the form re-renders at HTTP 200 with submitted values preserved, and nothing is written to the database"
    - "A slot left with no answer selected is rejected with the inline row error 'Choose Yes, No, or Maybe for this slot.' surfaced only on submit"
    - "While a participant is still selecting answers, no per-row error is shown; the every-slot-required rule is enforced only at submit time"
    - "A display name longer than 100 characters or a comment longer than 500 characters is rejected server-side (defense beyond the native maxlength) and nothing is written to the database"
    - "Opening a poll link whose participant token matches no poll returns HTTP 404 with the page copy 'This poll link isn't valid. Double-check the link you were given.' and no voting form"
  artifacts:
    - path: "internal/web/templates/notfound.html"
      provides: "Plain branded 404 page (no form, no nav) with the exact invalid-link copy"
      contains: "This poll link isn't valid."
    - path: "internal/web/server.go"
      provides: "Server-side validation in handleSubmitResponse + renderNotFound; 200 re-render on failure with preserved values and exact error copy"
      contains: "renderNotFound"
  key_links:
    - from: "internal/web/server.go"
      to: "internal/web/templates/vote.html"
      via: "handleSubmitResponse re-renders vote.html at HTTP 200 with NameError/row errors/BannerError populated"
      pattern: "renderVoteForm|BannerError"
    - from: "internal/web/server.go"
      to: "internal/web/templates/notfound.html"
      via: "handleVoteForm/handleSubmitResponse render notfound.html with status 404 on ErrNotFound"
      pattern: "renderNotFound"
---

## Phase Goal (MVP user story)

**As a** poll participant who was sent a link, **I want to** open it with no account, mark Yes/No/Maybe (with an optional comment) for every proposed slot, and come back later to change my answer, **so that** the organizer can see my availability and I can keep it up to date. (See 02-01-PLAN.md for the derivation.)

<objective>
Harden the voting slice proven by the tracer: enforce every CONTEXT.md validation rule server-side (name required, every slot answered, name/comment length caps) with the exact UI-SPEC error copy, re-rendering the form at HTTP 200 with submitted values preserved and nothing written to the DB on failure; and replace the tracer's plain http.NotFound stub with the branded invalid-link 404 page.

Purpose: The tracer proved the happy path; this plan makes the unhappy paths correct and safe (no silent DB writes on bad input, no XSS via re-rendered values, no naked 404).
Output: A validation-complete POST handler and a branded 404 page, both covered by Go tests asserting exact copy and no-DB-write-on-failure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-voting-end-to-end/02-CONTEXT.md
@.planning/phases/02-voting-end-to-end/02-UI-SPEC.md
@.planning/phases/02-voting-end-to-end/02-01-SUMMARY.md

# The handler/template/tests this plan extends (from 02-01) plus the Phase 1 validation pattern to mirror:
@internal/web/server.go
@internal/web/templates/vote.html
@internal/web/templates.go
@internal/web/server_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-side validation + 200 re-render with exact error copy</name>
  <files>internal/web/server.go, internal/web/templates/vote.html, internal/web/server_test.go</files>
  <read_first>
    - internal/web/server.go (handleSubmitResponse + voteFormView/voteSlotView from 02-01; the Phase 1 handleCreatePoll validate-then-re-render-200 pattern and bannerErrorCopy/rowErrorCopy constants to mirror)
    - internal/web/templates/vote.html (the form this task adds error placeholders to)
    - .planning/phases/02-voting-end-to-end/02-CONTEXT.md ("Validation & Confirmation Copy" — the exact rules and limits)
    - .planning/phases/02-voting-end-to-end/02-UI-SPEC.md (Copywriting Contract — the verbatim error strings)
    - internal/web/server_test.go (assertion style; the create-poll validation tests to mirror for shape)
  </read_first>
  <behavior>
    - TestVote_BlankName_RejectedNoWrite: POST with an empty/whitespace display_name re-renders at HTTP 200 (not a redirect), body contains "Your name is required." and "Check the highlighted fields and try again.", submitted answers are preserved in the re-render, and the participants table row count is unchanged (zero write).
    - TestVote_MissingSlotAnswer_RejectedNoWrite: POST with a valid name but one slot left unanswered re-renders at HTTP 200, body contains "Choose Yes, No, or Maybe for this slot." attached to the offending slot row, other slots' submitted answers preserved, and no DB write occurs.
    - TestVote_NameTooLong_RejectedNoWrite: POST with a display_name over 100 chars re-renders 200 with the banner and no DB write.
    - TestVote_CommentTooLong_RejectedNoWrite: POST with a comment over 500 chars re-renders 200 with the banner and no DB write.
    - TestVote_AllValid_StillPersists: a fully valid POST still 303-redirects and persists (guards against over-eager validation) — extend or reuse the tracer's happy-path assertions.
  </behavior>
  <action>
    Add exact-copy constants to internal/web/server.go alongside the Phase 1 ones: a name-required message "Your name is required.", a per-slot message "Choose Yes, No, or Maybe for this slot.", and reuse the existing bannerErrorCopy value "Check the highlighted fields and try again." (do not redeclare it — reference the constant Phase 1 already defines). Extend voteFormView with a NameError string and BannerError string, and voteSlotView with an Error string (mirroring createFormView/slotView). In handleSubmitResponse, after ParseForm and PollByParticipantToken, build the view from submitted values and validate BEFORE any DB write: (a) trimmed display_name must be non-empty else set NameError; (b) len([]rune(display_name)) must be <= 100 and len([]rune(comment)) must be <= 500 else set BannerError (these are the defense-in-depth server caps behind the native maxlength — a client can bypass maxlength); (c) for every slot, the answer_{slotID} value must be exactly one of yes/no/maybe else set that slot's Error to the per-slot message and preserve the (empty) selection. If any check fails, set BannerError to the banner copy, re-render vote.html at HTTP 200 via renderVoteForm (add this shared helper if the tracer inlined the render), preserve every submitted value (DisplayName, Comment, each slot's Selected), and return WITHOUT calling SaveResponse. Only when all checks pass proceed to the tracer's existing cookie/SaveResponse/303 path. Do NOT introduce any raw or unescaped HTML interpolation — re-rendered display_name/comment must round-trip through html/template auto-escaping (XSS mitigation T-02-04). Update vote.html to render {{.NameError}} inline under the name field, {{.BannerError}} as the banner above the form (hidden when empty, mirroring create.html's #form-banner pattern), and each slot's {{.Error}} inline under that slot row.

    Add the five tests named above to internal/web/server_test.go, asserting status codes, exact copy substrings, preserved values, and participants row counts via direct DB queries (mirror the Phase 1 no-write-on-failure tests).
  </action>
  <acceptance_criteria>
    - `go build ./...`, `go vet ./...` exit 0 and `go test ./internal/web/ -run 'Vote'` passes.
    - internal/web/server.go contains the literal strings "Your name is required." and "Choose Yes, No, or Maybe for this slot." and reuses the existing bannerErrorCopy constant (it is not redeclared).
    - TestVote_BlankName_RejectedNoWrite: an empty/whitespace display_name yields HTTP 200 (not 303), the body contains both "Your name is required." and "Check the highlighted fields and try again.", submitted answers are preserved, and the participants row count is unchanged.
    - TestVote_MissingSlotAnswer_RejectedNoWrite: one unanswered slot yields HTTP 200 with "Choose Yes, No, or Maybe for this slot." on the offending row, other answers preserved, no DB write.
    - TestVote_NameTooLong_RejectedNoWrite and TestVote_CommentTooLong_RejectedNoWrite: a display_name over 100 runes / comment over 500 runes each yields HTTP 200 with the banner and no DB write.
    - TestVote_AllValid_StillPersists: a fully valid submission returns 303 and persists.
    - No raw/unescaped HTML interpolation is introduced (`! grep -rn 'template.HTML' internal/web/`).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/cfiguerola/src/date-chooser && go build ./... && go vet ./... && go test ./internal/web/ -run 'Vote' -count=1 -v</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && grep -q "Your name is required." internal/web/server.go && grep -q "Choose Yes, No, or Maybe for this slot." internal/web/server.go</automated>
  </verify>
  <done>go build and go vet pass. All five validation tests pass. Blank name, missing slot answer, over-length name, and over-length comment each produce a 200 re-render with the exact UI-SPEC copy, preserved submitted values, and zero DB writes; a fully valid submission still 303-redirects and persists. Re-rendered name/comment values are html/template auto-escaped (no raw HTML interpolation introduced).</done>
</task>

<task type="auto">
  <name>Task 2: Branded invalid-link 404 page</name>
  <files>internal/web/templates/notfound.html, internal/web/templates.go, internal/web/server.go, internal/web/server_test.go</files>
  <read_first>
    - internal/web/templates.go (per-page composition; add a notfound template the same way vote was added in 02-01)
    - internal/web/templates/vote.html (layout/"content" define pattern to mirror)
    - internal/web/server.go (handleVoteForm/handleSubmitResponse ErrNotFound branches from 02-01 that currently call http.NotFound)
    - .planning/phases/02-voting-end-to-end/02-UI-SPEC.md (Copywriting Contract — invalid-link copy; "no form, no nav")
    - internal/web/server_test.go (assertion style)
  </read_first>
  <behavior>
    - GET /poll/{unknown-token} returns HTTP 404 and a rendered page whose body contains exactly "This poll link isn't valid. Double-check the link you were given." and does NOT contain the voting form (no display_name input, no "Save my response" button).
    - POST /poll/{unknown-token}/responses also returns the branded 404 (not a naked http.NotFound), since the token is invalid regardless of method.
    - The 404 page reuses the shared layout (title/canvas), carries no voting form and no navigation, per the UI-SPEC "plain 404 page".
  </behavior>
  <action>
    Create internal/web/templates/notfound.html defining "content": a single card with a heading and body text carrying the exact copy "This poll link isn't valid. Double-check the link you were given." — no form element, no links/nav. In internal/web/templates.go add a notfound field to pageTemplates parsed from layout.html + notfound.html (same per-page pattern as vote). In internal/web/server.go add a renderNotFound(w) helper that sets the status to http.StatusNotFound and executes the notfound template's "layout"; replace the http.NotFound(w, r) calls in the handleVoteForm and handleSubmitResponse ErrNotFound branches (from 02-01) with renderNotFound(w). Keep the status code 404. Because the "content" define is minimal here, the notfound template may need a nil-safe data value (pass an empty struct or nil consistently with how layout references fields — ensure the template executes without a runtime error). Add TestNotFound_InvalidPollLink to internal/web/server_test.go asserting a GET to an unknown participant token returns 404, the body contains the exact invalid-link copy, and the body does NOT contain "Save my response".
  </action>
  <acceptance_criteria>
    - internal/web/templates/notfound.html contains exactly "This poll link isn't valid. Double-check the link you were given." and has no `<form>` element.
    - internal/web/templates.go parses a notfound template (layout.html + notfound.html); internal/web/server.go defines renderNotFound and calls it (status 404) in both the handleVoteForm and handleSubmitResponse ErrNotFound branches (replacing the tracer's http.NotFound stub).
    - TestNotFound_InvalidPollLink: a GET to an unknown participant token returns HTTP 404, the body contains the exact invalid-link copy, and the body does NOT contain "Save my response".
    - `go build ./...`, `go vet ./...` exit 0 and `go test ./internal/web/ -run 'NotFound|Vote'` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/cfiguerola/src/date-chooser && go build ./... && go vet ./... && go test ./internal/web/ -run 'NotFound|Vote' -count=1</automated>
    <automated>cd /Users/cfiguerola/src/date-chooser && grep -q "This poll link isn't valid. Double-check the link you were given." internal/web/templates/notfound.html && grep -q 'renderNotFound' internal/web/server.go</automated>
  </verify>
  <done>An unknown participant token (GET or POST) returns HTTP 404 rendering the branded page with the exact invalid-link copy and no voting form or nav. TestNotFound_InvalidPollLink passes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (02-02) produces:

**Go — internal/web/server.go:** name-required + per-slot error constants; NameError/BannerError on voteFormView, Error on voteSlotView; server-side validation (name required, name<=100, comment<=500, every slot answered) with 200 re-render + preserved values + zero-write-on-failure; `renderNotFound` helper; ErrNotFound branches now render the branded page.

**Templates:** internal/web/templates/notfound.html (+ templates.go `notfound` field); vote.html gains inline NameError, banner, and per-slot Error placeholders.

**Tests:** TestVote_BlankName_RejectedNoWrite, TestVote_MissingSlotAnswer_RejectedNoWrite, TestVote_NameTooLong_RejectedNoWrite, TestVote_CommentTooLong_RejectedNoWrite, TestVote_AllValid_StillPersists, TestNotFound_InvalidPollLink.

(Full phase artifact surface is enumerated in 02-01-PLAN.md.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant browser -> HTTP server | Untrusted display_name/comment/answer values re-rendered back into HTML on validation failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering (stored/reflected XSS) | vote.html re-render (server.go) | high | mitigate | Re-rendered display_name/comment/answers pass through html/template auto-escaping; no template.HTML or raw interpolation introduced. Verified by the negative gate below |
| T-02-06 | Denial of service | handleSubmitResponse body (server.go) | medium | mitigate | POST body bounded by http.MaxBytesReader(maxFormBytes) — reused from the tracer/Phase 1 pattern; oversize trips 413, not a panic |
| T-02-07 | Tampering | answer allow-list (server.go) | medium | mitigate | Handler rejects any answer outside {yes,no,maybe} server-side with the per-slot error and no DB write, in addition to the DB CHECK backstop from 02-01 |
| T-02-08 | Info disclosure | 404 page (notfound.html) | low | accept | The branded 404 states only that the link is invalid; it discloses no poll existence/enumeration signal beyond what an unguessable-token miss already implies. Accepted — same token-entropy model as Phase 1 T-01-05 |

Cross-cutting negative gate (T-02-04): no raw/unescaped HTML interpolation is introduced in the web package. Verified in <verification>.
</threat_model>

<verification>
```
cd /Users/cfiguerola/src/date-chooser
go build ./... && go vet ./...
go test ./internal/web/ -run 'Vote|NotFound' -count=1
# T-02-04 negative gate: no raw HTML interpolation helper introduced this phase
! grep -rn 'template.HTML' internal/web/
# Exact copy present
grep -q "This poll link isn't valid. Double-check the link you were given." internal/web/templates/notfound.html
```
</verification>

<success_criteria>
- go build ./... and go vet ./... exit 0; the Vote and NotFound tests pass.
- Every invalid submission (blank name, missing slot answer, over-length name/comment) re-renders at HTTP 200 with the exact UI-SPEC copy, preserved values, and no DB write.
- A valid submission still persists and 303-redirects.
- An unknown poll link returns HTTP 404 with the branded invalid-link page (no form/nav).
- No raw/unescaped HTML interpolation exists in internal/web (T-02-04 gate passes).
</success_criteria>

<output>
Create `.planning/phases/02-voting-end-to-end/02-02-SUMMARY.md` when done.
</output>
